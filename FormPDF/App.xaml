<Application x:Class="FormPDF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FormPDF"
             StartupUri="/View/MainWindow.xaml">
    
    <Application.Resources>
        
        <!--Definindo as estilizações principais do App-->

        <SolidColorBrush x:Key="LabelBrush" Color="#F5FBE6"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#233D4D"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Javanese Text"/>
        </Style>

        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TitleBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource LabelBrush}"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontFamily" Value="Verdana"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="FontFamily" Value="Calibri"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

    </Application.Resources>
    
</Application>
